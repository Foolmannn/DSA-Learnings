#include <iostream>
using namespace std;

int main(){
    cout << "Suman " << "\n" <<"Pun Magar " << endl;

    return 0;
}
// we can combine the multiple commands using && for ex

// g++ code.cpp -o code.exe && ./code.exe